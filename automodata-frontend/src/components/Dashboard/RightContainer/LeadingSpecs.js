import React from 'react';
import styles from './LeadingSpecs.module.css';

const LeadingSpecs = () => {
    return (
        <div className={styles['leading-specs-section']}>
        </div>
    );
}

export default LeadingSpecs;